/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com', 'jsonplaceholder.typicode.com', 'images.pexels.com', 'images.unsplash.com', 'i.pravatar.cc'],
  },
};

module.exports = nextConfig; 